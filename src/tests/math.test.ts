
describe("math.test", () => {
    test('suma basica', () => {
        expect(1 + 1).toBe(2);
    });
    test('resta basica', () => {
        expect(1 - 1).toBe(0);
    });
    test('mutplicación basica', () => {
        expect(2 * 2).toBe(4);
    });
})